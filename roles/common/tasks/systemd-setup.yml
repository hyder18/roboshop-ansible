


- name: Copy systemd service file
  ansible.builtin.copy:
    src: "{{ component }}.service"
    dest: /etc/systemd/system/${component}.service

- name: reload systemd setup
  ansible.builtin.systemd:
    state: restarted
    name: "{{ component }}"
    enabled: true
    daemon-reload: true
